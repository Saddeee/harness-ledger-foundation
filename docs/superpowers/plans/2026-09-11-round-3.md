# Round 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Split Instructions and Skills, show what changed per version, un-hide decisions, move Edit into the card, add prev/next browsing, add AI-analysis and per-project settings, and ship removable demo data.

**Spec:** `docs/superpowers/specs/2026-09-11-round-3-instructions-skills-settings.md` (and the two earlier specs it extends).

## Global Constraints
Same as the previous plan: Node 22; `npm run harness:build` after `harness/src` changes; only the harness routes are fetched (`improvements, runtime, knowledge, executor, projects`, plus `skills` added here); POST actions unchanged except new settings/project-settings actions; no credit-spending or Skill-writing tool; keys never in SQLite, never in responses beyond `has_key` + last four; no internal vocabulary or spec mentions in UI; every `<details>` collapsed; touched files eslint-clean; commit trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_016MGZ592H9Z87KgxZERZcVz`; branch `local-harness-dev`.

### Task 1 — Data layer v8, diff, demo seed (harness)
Files: `harness/src/migrations.ts` (v8), `harness/src/store.ts` (settings keys + validation: budget 1–1000, max rules 1–50; `getProjectSettings(projectId)`, `setProjectSettings(projectId, patch)`, `effectiveMaxActiveRules(projectId)`, `listLlmCalls(month)`, `sumLlmCostThisMonth()`), `harness/src/llm-keys.ts` (`setKey(provider, key)`, `removeKey(provider)`, `keyStatus()` → `{ [provider]: { has_key, last4 } }`, file 0600 under the data dir), `harness/src/diff.ts` (`lineDiff(a, b, context=2)` → `{ added, removed, lines }`, LCS on lines), `harness/src/knowledge.ts` (compose takes `maxActiveRules`; preview gets `over_rules`), `harness/src/improvements.ts` (preview `over_rules` from `effectiveMaxActiveRules`), `harness/src/executor/beats.ts` (`executeWrites` skips project targets with `auto_write = 0`, counts `skipped_auto_write`), `harness/src/demo.ts` + npm scripts `demo` / root `harness:demo` (`--add`, `--remove`, `--status`), `harness/src/adapter.ts` re-exports, tests for each (store, diff, demo add/remove leaves real rows untouched, executor skip).

### Task 2 — Routes and client
Files: `src/routes/api/public/harness/knowledge.ts` (versions gain `changes`; response gains `demo_loaded`), new `skills.ts` route (GET: skills with per-skill snapshot history), `executor.ts` (GET adds `llm: { provider, models, monthly_budget_usd, spent_usd, keys }`; POST actions `llm_settings`, `llm_key { provider, key }`, `llm_key_remove { provider }`, `defaults { max_active_rules }`), `projects.ts` (GET rows gain `settings`; POST `project_settings { lovable_project_id, max_active_rules|null, auto_write }`), `src/lib/improvements-client.ts` helpers/types, `src/lib/server/harness-runtime.ts` if new dist modules are needed. Structural tests.

### Task 3 — UI
Files: nav in `route.tsx`; `instructions.tsx` (from `knowledge.tsx`, minus Skills, plus "What changed" per version and the demo notice), `skills.tsx`, `knowledge.tsx` + `versions.tsx` redirect to `/instructions`; `improvement.tsx` (inline decision buttons, Edit button in the blockquote, prev/next header with keyboard), `inbox.tsx`/`ledger.tsx` pass the ordered id list to the detail; `local-settings.tsx` (AI analysis + Defaults sections), `local-projects.tsx` (expandable per-project settings), Add dialog `over_rules` message. New tests in `harness/test/ux-round3.test.ts`; minimal in-place edits elsewhere.

### Task 4 — Verification
Build, typecheck, tests, scoped eslint, executor `--status`, `harness:demo --add` then `--status` then Instructions/Skills pages via curl 200, `--remove` leaves the real row count unchanged.
