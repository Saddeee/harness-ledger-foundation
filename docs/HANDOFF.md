# Harness Ledger — handoff (updated 2026-09-18 after the truth checkpoint; see §0)

## 0. Checkpoint 2026-09-18 (read this first; the sections below it are older and partly superseded)

Start with `PLAN.md`, `DECISIONS.md`, `VERIFICATION.md`, `SPEC.md`, `DEMO_PLAN.md`, `build-log.md` at the
repo root and the audits under `docs/audit/`. Branch `local-harness-dev` at the checkpoint's last commit;
`main` is still the Lovable-built state `3fff0d9`, now also tagged `hosted-foundation-v1` (pushed). Nothing
was merged, pushed (except the tag) or republished. No Lovable operation ran; 0 credits spent.

What changed, in one screen:

- **The test is a historical replay.** "Paired test", "proof" and "both builds" are gone from copy and README.
  Labels: Historical result / Replay with rule. Migration v18 records for every run which Project Knowledge
  the copy started from (exact / nearest earlier / today's / none), keeps the rules that were live at the time
  (the old composer dropped them: runs 4 and 7 lost the kronor rule), and a quality label. Runs 1–7 were
  backfilled: 3, 5, 6 had silently used today's Knowledge. The judging page shows the environment and an
  evidence-strength line; technical details are collapsed. Paired comparison is deferred (DECISIONS D3).
- **Managed block** now reads `## Instructions managed by Harness Ledger` plus a conflict note; blocks
  written under the old heading are still recognised as Harness Ledger's own (bullet-line comparison).
- **Skills** are a first-class destination: the Rule writer proposes Knowledge / Skill / both with a reason
  and a SKILL.md draft (migration v19); the user can change the destination, edit, approve, retire and restore
  a proposal locally. Writing a Skill to Lovable is NOT wired (REST path deprecated by Lovable; MCP tools
  exist; needs one approved live write to verify). The UI, README and landing page say so.
- **Analysis** records a context packet per model call (migration v20: ids, reasons, size, truncation,
  strategy version), includes live rules, Skill names and older term-matched messages; "Reanalyse history" is
  a separate, scoped, estimated action that never overwrites a human decision (disagreement review cards in
  the Inbox). `automatic_analysis_after_sync` is a new setting, default off (Settings › Sync schedule).
- **Rule usefulness** (migration v21): verdicts are Keep / Review / Retire / Not sure; observed repeat
  corrections and AI review are separate lines; inactivity opens "Review for relevance", never a retirement;
  an opposite request is classified (one-task exception etc.) before it questions a rule.
- **History**: "Restored Knowledge from version N" with reason, restored-from, rules added/removed.
- **Harness MCP** rewritten: 13 tools over the adapter (same permissions as the buttons); the raw store tools
  that could fabricate a written state are gone; the old unauthenticated `harness/src/web` server is deleted.
- **Setup**: `npm run setup` and `npm run harness:start` (127.0.0.1, repo-local DB). README rewritten with a
  truth test (`harness/test/readme-truth.test.ts`). Landing page rebuilt (`src/lib/landing-copy.ts`).
- Verification: 852 tests, both typechecks, lint (0 errors), production build (no better-sqlite3 in
  `.output`), hosted-mode and local-runtime smoke on spare ports, fresh clone setup in 21 s.

**Restart the owner's dev server** to pick up migrations v18–v21 (they apply on first start and are additive
except the verdict/health table rebuilds, which keep every row). Back up `harness/data/harness.db` first.

Owner decisions still needed: approve the demo fixture plan (`DEMO_PLAN.md`, Nordic Booking Desk, about
8–16 credits), whether to merge `local-harness-dev` to `main` (see the release report in the session
summary), the first paid Skill write, and whether to build the paired comparison next.

---

For the next agent. Everything here comes from the working sessions of 2026-09-11 → 2026-09-13 with the owner (Lovable workspace `937baaeb85dfcb22e8b2`, project `28bd5471-78e0-43af-a29b-5018198cb13c`). Read this before touching anything. The owner's rules at the end are binding.

## 1. What the product is, in one paragraph

Harness Ledger makes a Lovable user's builder agent better over time. It reads the user's own chat history with Lovable, finds where they corrected Lovable, proposes one standing instruction per correction, and — once the user approves — writes it into the project's or workspace's Lovable **Knowledge**, keeping every version and letting them roll back. It then watches later real builds to see whether each rule helped, hurt, went unused or was contradicted, and proposes retiring the ones that don't earn their place. Autonomy is the sell point, but *nothing spends Lovable credits and nothing spends LLM tokens unless the user pressed a button*. Every number shown is labelled with what produced it.

## 2. Vocabulary (Round 5)

| Concept | UI word | Code / docs word |
|---|---|---|
| Reading chats, Knowledge and Skills from Lovable on a schedule; no model | **Sync** | sync |
| The model step that reads synced chats and proposes a rule | **Analysis** ("Analyse now", "AI analysis" in Settings) | analysis; roles are **Classifier**, **Rule writer**, **Judge** |
| A rule the analysis proposes and the user has not decided on | **Suggestion** | suggestion / improvement (internal view name stays) |
| A suggestion the user accepted | **Rule** | rule |

Nav label changed from "Improvements" to "Suggestions"; `/ledger` stays the same. `/improvements` and `/suggestions` redirect to `/ledger`. Role label "Miner" renamed to "Rule writer"; "Judge" role added. `proveCostLine()` now returns "Uses Lovable credits like any build; the cost is recorded after the test." without parameters.

## 3. Where the code is

Repo: this repository, branch `local-harness-dev` (about 125 commits ahead of `main` at head `674927b` after round 6; the owner never chose merge / PR / keep — ask; note the Co-Authored-By trailers on round-5 commits name whichever model wrote each commit, all with the same Claude-Session link). Two halves:

- `src/` — the web app (TanStack Start + React 19 + shadcn, Supabase auth). File routes under `src/routes/_authenticated/`: `inbox`, `ledger` (labelled "Suggestions"), `instructions`, `history`, `skills`, `projects`, `settings`; `knowledge`/`versions`/`overview`/`improvements`/`suggestions` are redirects. Server routes under `src/routes/api/public/harness/`: `improvements`, `runtime`, `knowledge`, `skills`, `executor`, `projects` — the UI may fetch **only** these six (a structural test enforces it). They bridge to the local package through `src/lib/server/harness-runtime.ts` (dynamic import of `harness/dist/*` when `HARNESS_RUNTIME=local`; hosted mode gets a polite "runs on your machine" body).
- `harness/` — standalone Node package (SQLite via better-sqlite3, `node:test` via `tsx`). Key modules: `src/store.ts` (all SQL; new work is appended in delimited `// ---- Round N Task ----` blocks), `src/migrations.ts` (v1–v17, one array; **one task owns each new migration**), `src/improvements.ts` (the "improvement" view = correction + rule + versions, all POST actions), `src/knowledge.ts` (managed block compose, sha256, cap), `src/adapter.ts` (the only module the web app may import), `src/mcp-server.ts` (the Harness MCP server Claude Code sessions used before the executor existed), `src/executor/` (Lovable OAuth + MCP client, REST client `lovable-rest.ts`, lock, beats incl. inline writes, experiments = the paired-test runner, scheduler, CLI), `src/analysis/` (classify, segment, propose = the Rule writer, auto-accept, adherence = the Judge, health, retire, run), `src/llm/` (provider client), `src/llm-keys.ts`, `src/diff.ts`, `src/demo.ts`.
- Docs that are the source of truth (newest wins where they disagree): `docs/superpowers/specs/2026-09-1[12]-*.md` (five rounds; round 5 is `2026-09-12-round-5-clarity-automation-history.md`), `docs/superpowers/specs/explorations/*.md` (three design studies), `docs/superpowers/plans/2026-09-1[12]-*.md`. The owner's original product spec lives outside the repo (a private SPEC.md); treat it as intent only, it produced the earlier mistakes. **Never mention specs, "SPEC.md", "checkpoint" or "Claude Code" (except as the provider name) in UI copy.**

Run it: Node 22 (`nvm use`), `npm i`, `npm run harness:install && npm run harness:build`, then `HARNESS_RUNTIME=local HARNESS_DB_PATH=harness/data/harness.db npm run dev`. Since Round 6 the app process runs the hourly schedule itself (lock file `harness/data/executor.lock`, owner "app"); `npm run harness:executor` is optional for headless use and refuses while the app holds the lock. **Restart the dev server after `npm run harness:build`** — the server caches the harness modules per process. Tests: `cd harness && npm test` (732 pass), `npm run typecheck` in both packages, `npm run build`. Repo-wide `npm run lint` has ~1,400 pre-existing Prettier errors in files this work never touched; the rule was "every touched file is eslint-clean", and it is.

## 4. What was built, round by round (all on this branch)

1. **Simplification pass** — public landing page at `/`, one nav for both runtimes, one `DecisionCard` used by Inbox / Improvements / detail, add-or-skip straight from the list, detail trimmed, Overview folded into Inbox.
2. **Local executor + Knowledge page** — the executor holds its **own** Lovable OAuth grant (dynamic client registration with a loopback redirect on port 8765, tokens in `harness/data/lovable-auth.json`, mode 0600) and talks to `https://mcp.lovable.dev/` with `@modelcontextprotocol/sdk`. Beats: sync chat history (redacted, resumable cursor), snapshot Knowledge and Skills, stage writes for approved items, execute pending writes (sha check → written / stale / failed / cancelled), recompute rule health, propose retirements. Scheduler: hourly within a window (defaults 10–22, Settings), plus "Sync now". Only eight Lovable tools are reachable by construction (get_me, list_projects, list_messages, get/set project knowledge, get/set workspace knowledge, list_workspace_skills). `--connect`, `--once`, `--status`, `--analyse`, `--disconnect`. The owner **has connected** (`--status` says connected) but had run no sync pass by the end of the session.
3. **Instructions & Skills pages, change view, settings** — "Instructions" (current Knowledge text per project + workspace, rules live in it, version history with a real line diff "What changed", restore, pending-write banner) and "Skills" (read-only). Settings: sync schedule, Knowledge limit, AI analysis (provider incl. **Claude Code**, key, per-role model with hints, monthly **token** budget), defaults, notifications; per-project max active rules and auto-write. Demo data: `npm run harness:demo -- --add|--remove|--status` (currently **loaded**; removal is exact and tested).
4. **Analysis + outcomes + deprecation** — `harness/src/llm/` (OpenAI/Anthropic/Google via plain fetch with strict-mode-compatible JSON schemas, or the local `claude -p` CLI on the user's subscription; every attempt logged to `llm_calls`; token budget checked before every call incl. retries; a guard `assertStrictCompatible` refuses bad schemas). `harness/src/analysis/`: classify (fake-injectable `CallLlm`), segment into task episodes, mine proposals with evidence validation, per-target dedupe (bigram Dice) and a verification plan per rule; `runAnalysis` with a 200-call cap and `analysis_requests`/`analysis_runs` bookkeeping; the loop runs it only when a request exists. `rule_health` from real builds; retirement proposals as `kind: "retire"` Inbox items (negative ids) with Retire / Keep (snooze 30 d) / Re-add (resets the health window). Small gaps: Open-in-Lovable links, sidebar count, "New" marker, browser notification opt-in.
5. **Round 5 (2026-09-12): clarity, automation, history, honest proof** — vocabulary (Sync fetches, Analysis is the model step with roles Classifier / Rule writer / Judge; undecided items are Suggestions, accepted ones Rules; "miner" and "improvement" are gone from UI copy and a structural leak-detector test keeps them out). Inbox = compact decision cards only, opening the Suggestions detail. Instructions = a rules table per target (status / since / observed / verdict / adherence) with the raw Knowledge text collapsed below. New **History** page: one timeline per project/workspace (versions, changes made in Lovable outside Harness Ledger, decisions incl. automatic ones, skill snapshots, verdicts; selecting a node shows its full text, with "Show as diff"). **Decision mode** setting: `ask` (default) or `automatic` (accept when confidence ≥ 0.8, no duplicate/contradiction, under caps; unsure items go to the Inbox with the reason; `decided_by='automatic'` marker; retirement proposals never auto-applied). **Feedback loop**: skip reasons, accepted/skipped/wording-edit history in the Rule writer prompt (clamped, framed as data), a re-proposal guard (Dice ≥ 0.8 vs skipped), Inbox ranking by confidence × tag acceptance rate, a feedback line in Settings. **Evidence**: "helped" removed from copy; per-rule one-click verdicts (`rule_verdicts`, derived into health on every recompute); the **Judge** role reads Lovable's real reply per applicable episode and records followed / broke / not_applicable with a quote (`rule_adherence`, ≤50 calls per Analyse-now run); Settings › Evidence chooses which sources count (paired tests disabled until Phase B); `runAnalysis` now recomputes health and proposes retirements at its end. AI analysis settings show one provider+model by default with per-role rows under Advanced. Demo data (`npm run harness:demo -- --add`) now seeds 11 suggestions across every group except "Waiting to be written" (deliberately: the demo must never leave anything the executor would write to the real project), ~13 versions incl. a restore and a stale write, an outside edit, two skills × 3 snapshots, computed `rule_health`, verdicts and adherence rows; removal is provenance-checked and tested to be exact.

6. **Round 6 (2026-09-12 → 13): wired writes, controls, clean cards, demo isolation, the paired test, the Tests page.** Spec `docs/superpowers/specs/2026-09-12-round-6-wired-writes-controls-paired-tests.md`, plan `docs/superpowers/plans/2026-09-12-round-6.md`. Tasks 1–5 as before (migration v12; `lovable-rest.ts` from the SDK 1.7.0 types with a fake REST server; `lock.ts`; every pressed decision writes to Lovable in the same request via `improvementActionAndWrite` in `beats.ts` with fresh read / recompose-on-drift / retry recognition / 45 s budget; "Sync now" inline; scheduler in the app under the lock; Undo / Cancel / Remove from Knowledge, Restore only on History; one action bar per card, inline one-per-rule verdict with a visible effect; demo isolation). **Paired test (Tasks 6a + 6b + fix):** `experiments.ts` runner (copy the project at the moment before the request via `POST /v1/projects` with `source_project_id`, Knowledge = snapshot-at-episode + rule, `chat`, poll, record summary/diff/cost, delete the copy; only `completed` builds are judgeable; full request replay; one run at a time; the REST message id is resolved by role + normalised content because the MCP sync stores `main:user#…` ids while REST uses `umsg_`/`aimsg_`; the REST token is refreshed through the MCP provider when older than 8 h; corrections fall back to follow-up user/operator messages when an episode has no classified corrections), `experiments-queue.ts` (background run in the app process, crash recovery after 20 min), `test`/`judge`/`feedback` actions, `/judge?run=` judging screen (two columns, per-correction "Still needed?", score = no ÷ corrections, feedback box), **`/tests` page** (every run with status, cost when measured, result, per-test feedback; copies to delete by hand), Settings › Lovable credits (budget 12/month, keep-copies), History `test` node, `rule_health` paired gating. **Round 6c:** Suggestions holds only open decisions ("Open", waiting groups, collapsed "Decided earlier"); live rules are on Instructions; the workspace section is titled "All your projects (workspace Knowledge)" with an explanation; Claude Code saves with default model `sonnet`; Analyse now shows why it is disabled. Migration v13 adds `experiment_runs.feedback`. **Live on the owner's project (2026-09-13):** the owner's first "Test this rule" press failed at the copy step with Lovable's `invalid_message_id` (the MCP-id problem above, now fixed); Remove → Re-add round-trip verified (versions 28/29, block present once on Lovable). **The first successful paired test has not run yet — the owner presses it.**

## 4b. Round 7 (2026-09-13): live end-to-end test, then proof you can look at

Round 7 is committed on `local-harness-dev` (not pushed; merge or PR is the owner's call); `cd harness && npm test` = 732 pass, both typechecks clean, `npm run build` passes, every touched `src/` file is eslint-clean (one pre-existing fast-refresh warning in `improvement.tsx`).

**How it was tested.** Headless Playwright against a second dev server on port 18500 (same DB), signed in as a throwaway Supabase account `harness-e2e-20260913@example.com` (sign-ups auto-confirm). Chromium needs five libs this box lacks — see the memory note `e2e-test-fixtures-2026-09-13`. A real Lovable test project **"Quick Tip Calculator"** (`96a42c68-61bf-4da0-9793-88281ea0224c`) was created and chatted with; the owner capped credits at 15, **12.8 were spent** (project + chats ~9.3, five paired tests 0.6/0.3/0.5/0.8 + the owner's own 2.3-credit Lumble test not counted). Analysis ran with Claude Code.

**Bugs found live and fixed (each with a test):**
- Paired test replayed a SPEC note instead of the real request (SQLite sorts NULL `occurred_at` first) → shared `EPISODE_REQUEST_ORDER` (first user message).
- Settings › AI analysis never saved (page sent `llm_provider`/`llm_models`, route read `provider`/`models`; toast still said saved); the two provider selects now move together.
- Lovable MCP returns the literal `(empty)` for empty Knowledge → it was written into Knowledge; normalised in the MCP client and the composer (residue dropped).
- Remove right after Add went stale ("Someone edited the Harness Ledger block") and the card still said removed: the read-back is now recorded as the latest snapshot, and a live block equal to Harness Ledger's last written block is recognised as Harness Ledger's own. Failed writes toast as errors.
- Timestamp compares across SQLite `YYYY-MM-DD HH:MM:SS` vs ISO `…T…Z`: rule health counted builds from *before* a rule was written ("4 builds since added"), the Judge judged them, and the test copy's Knowledge base could come from after the request → `julianday()` in SQL, UTC-safe parsing in the runner.
- Paired-test recording: replies stored as raw `<lov-tool-use>` markup (Lovable's reply format changed; `humanVisibleText`/`lovableReplyText` now strip tool blocks), the "original build" included replies to later corrections, "0 edits since" (the real `/edits` rejects `limit>50`; now paged with `before`, and unknown is not shown as 0), corrections listed out of order.
- A missing Rule-writer confidence became "0.00" and blocked automatic mode (now null + honest "gave no confidence"; the prompt defines confidence). Automatic writes were attributed to "YOU" in History.
- An all-"Unclear" test verdict counted as a hurt in rule health (now only decided verdicts count). Credits showed "3.6999999999999997".
- Projects page: blank project names (MCP `display_name`), hosted component 500 while loading; cards showed raw project ids and repeated the instruction as the title (now a clipped title).
- Tests page rows opened the suggestion instead of the test (owner's complaint) → whole row opens `/judge?run=`; Project column; build links.
- Skills deleted in Lovable stayed "current" forever → deletion snapshots (migration v15), Skills page drops them, History "Skill X deleted".

**Owner decisions implemented in Round 7:**
- **Proof you can look at.** A test now makes the paid "with the rule" copy *and* (checkbox, default on, free) a copy of the project right after the original request — remix `including`, no chat. The judging screen has two identical columns (screenshot tied to the commit, "Open the app", "Open in Lovable", Lovable's summary, reply, diff, "Delete copy" with confirm). **Copies are kept by default** (setting "Keep test builds as projects"; a failed test's copies are always deleted); screenshots stay after a copy is deleted. The Tests page links to the builds.
- **"Test it first" adds nothing**: the Add dialog offers "Add it now" or "Test it first"; testing never adds; add from the judged test.
- **One suggestion per correction.** The Rule writer is called once per uncovered correction (a proposal may cite other corrections asking for the same thing); `correction_mining` (migration v14) remembers what was asked so "no rule here" is not re-asked. Live: the dropped fonts correction became its own suggestion. The judging screen asks only about the rule's own corrections and saves them with the verdict.
- **No empty Harness Ledger block.** Removing the last rule removes the whole block (the owner's workspace Knowledge had an empty heading; it is empty again).
- **Workspace wording warning** in the Add dialog when a rule for all projects says "this app"; the Rule writer is told not to word workspace rules that way.
- **"You asked for the opposite" retirement** (migration v16 rebuilds `retire_proposals` with reason `changed_mind`): the classifier now sees the project's live rules and returns `contradicts_rule_ids`; a message going against a live rule opens a Retire/Keep card quoting that message. Live: "switch to euros" and "all lowercase" opened cards for the kronor and sentence-case rules; Keep and Retire both verified (Retire removed kronor from Lovable). `npm run llm:smoke` passes with the new classifier schema.

**2026-09-14 follow-up (owner review + code review):**
- A `/code-review high` over the uncommitted Round 7 diff found three real bugs, all fixed with tests: (1) retrying an older failed write after a newer one could drop the newer rule or re-add a retired one (the own-block branch now merges with live, still-active rules); (2) the classifier's "asked for the opposite" check also saw suggestions still in the Inbox (now only rules that are active and written); (3) an empty/malformed/paged skills answer could mark every skill deleted (`skillListFromResponse` reports `complete`; deletions only from a complete answer). A scoped Sonnet re-review confirmed (2) and (3).
- Owner: History's "Restore this version" is now **"Undo this change"** (newest written version, now allowed) and **"Go back to before this change"** (older ones; the dialog says later changes are undone too); nodes read "Went back to before version #N". While testing it, found that going back to an older version **silently did nothing** (it recomposed today's rules; pre-existing): a go-back now writes exactly that text when Lovable still holds Harness Ledger's last write, refuses (stale) when Knowledge changed outside Harness Ledger, and reconciles rule states (lines gone → Reverted, lines back → active). Verified live both ways on Quick Tip Calculator.
- Found live: Lovable's MCP returned an **error result as text** ("Lovable API error: 401 unauthorized") and Harness Ledger stored it as the project's Knowledge (a write in that moment would have put it into Lovable). `parseToolResult` now throws on `isError`; inline writes refresh a near-expiry token first. The one corrupt snapshot row was deleted.
- A second scoped re-review of those fixes found two more, fixed with tests: rule-state reconciliation after a go-back matched only a rule's current wording (undoing a wording change, or a multi-line instruction, wrongly read "Reverted") — it now matches every wording in `rule_revisions` as the rule's own block entry; and the pre-write token refresh had no time limit (now bounded to 15 s).
- History diffs no longer use a stale/failed attempt as the baseline, and same-second versions keep their real order (the "+0 −0 lines" pairs).
- Test copies are now named "Harness Ledger test 7 · with the rule · Quick Tip Calculator" (Lovable refuses "#" and other characters in display names; the name is sanitised). The kept copy was renamed.
- Owner questions answered: the kept copy has exactly one exchange because the copy starts at the moment before the original request, without history, and Harness Ledger replays that one request; test 7's "original build" copy was deleted on purpose to test "Delete copy"; test 6's copies were deleted because a Settings test had saved "keep copies = off" (now on).

**2026-09-14, second session (owner requests):**
- Round 7 committed as `8904a40` (not pushed).
- **Naming:** the product is always "Harness Ledger" in anything a person reads — UI copy, server messages, test copy names ("Harness Ledger test 7 · with the rule · Project"), READMEs, this handoff. Done with a TypeScript-AST pass over string/JSX/template literals only (identifiers, comments, imports, the `harness:start` markers and applied migrations untouched); structural tests updated the same way. The `harness/` folder, code identifiers and the "Harness MCP" server name are unchanged.
- **Analyse now progress** (migration v17 `analysis_runs.progress_json`): the run records its step (classify / group / rules / judge / health) and "done of total"; the Inbox and Instructions notice polls every 2 s while queued or running and shows a bar, the step list with "N of M", elapsed time, and "Only new messages and corrections are analysed". Pressing Analyse now starts the run immediately in the process that holds the schedule lock (no more up-to-a-minute wait); lists refresh when it finishes. Verified live.
- **What Analyse now processes:** only unclassified messages, only corrections no suggestion covers and not already asked about, only builds not yet judged per rule; rule health is recomputed for all rules.
- **Test copies on Projects** carry a "Test copy · test N" label, and names of allowed projects follow renames in Lovable (the owner had allowed test 7's copy, which is why it appeared on History).
- **Public README** rewritten (what it is, the loop, proof and its limits, why it runs locally — no Lovable API for a hosted third-party app, hosted OAuth client rejected — safety/cost, page-by-page guide, setup, architecture, development, roadmap, privacy, FAQ), with five real screenshots in `docs/images/` (test project only); on 2026-09-14 the owner asked for a shorter README (keep the explanation and architecture), so it was cut to about half. On 2026-09-15 the owner asked that setup be easy to follow from the README: Getting started is now six numbered steps (install/start, app account via Sign up, Connect Lovable, choose projects and sync, AI provider, Analyse and decide) plus requirements, demo, CLI and a troubleshooting table. Following it on a fresh clone showed plain `npm install` failed (ERESOLVE: `@lovable.dev/sdk` optional peer zod 4 vs the app's zod 3), so a root `.npmrc` with `legacy-peer-deps=true` was added; the fresh clone then installed, built and served `/login`. Branch pushed to origin/local-harness-dev. Review-readiness pass (same day, owner asked that following the README must just work): a clone from GitHub was set up and walked through every README step in a headless browser — sign up (new throwaway app login `harness-readme-20260915@example.com`), demo add/tour/remove, Connect Lovable (opens Lovable's real login; approval needs the owner, so steps 4–6 used this machine's existing Lovable grant via `HARNESS_AUTH_PATH`), allow Quick Tip Calculator + Sync now (22 messages), Claude Code saved, Analyse now with live progress (11 messages, 3 suggestions, ~2.5 min), Skip, and every page. Add/Test were not repeated from the fresh DB (they write to the live test project, whose block belongs to the main DB; both were verified live in Round 7). Fixes from that pass: `npm run lint` now passes repo-wide (ignores `harness/dist` and Lovable-generated `src/integrations/supabase`, Prettier applied, no logic changes); `retire.test.ts` had a fixture dated 2026-09-15 that went stale today (now relative to the clock); the demo creates and removes its own "Demo project" when none is allowed (it used to fail on a fresh install) and only prints the isolation warning when it added data; Projects shows an "Open it here" link if the Lovable login tab was blocked; "No API key saved for OpenAI" (was `openai`); "1 Knowledge snapshot" plural; History's empty state no longer promises skipped suggestions; root `package-lock.json` ignored. A Sonnet reviewer cross-checked every README command, label, env var, default and anchor (all correct except the test count, now "700+"); its clarity notes were applied. Repo scrubbed of the owner's email and local paths; no LICENSE yet (owner's call).

**Found but not fixed — decide/design next:**
- **Proof confounder (important):** the test copy inherits Lovable's *current* project memory (`mem://` notes), not the memory at the time of the request. Live, the copy for a 19:14 request came out in euros and lowercase — preferences given at 23:08. The judging screen now says so. Options: clear/restore the copy's memory before the replay if the API allows, or state it per test.
- **Behavioural checks** (owner's "a rule not to break login" example): screenshots prove visual rules only. Proposed next step: per-rule checks Harness Ledger runs against both copies' previews (e.g. Playwright: open `/login`, sign in) — `verification_definitions` tables already exist.
- The owner's Lovable message list lags ~20 s after a chat (a sync right after chatting can miss messages; the next sync picks them up).
- The owner's app process (port 8081) holds the schedule lock and runs *Analyse now* with the code it loaded; **restart it** to pick up Round 7 (migrations v14–v17 are already applied to `harness/data/harness.db`).

## 5. Current state of the owner's data (2026-09-13, end of Round 7)

Schema version **17**. Allowed projects: Harness Ledger Foundation, **Quick Tip Calculator** (test project, see above), and — allowed by the owner during Round 7 — Lumble Agent Explorer and Frontier Forge. LLM provider: **Claude Code / sonnet** for every role. Decision mode `ask`; all numeric settings at defaults; `keep_test_copies` true.

Rules on Quick Tip Calculator: sentence case (rule 24, live, retirement kept/snoozed), kronor (rule 22, Reverted — retired from the "euros" card, then undone and re-undone while testing History), reset-everything (rule 23, workspace, retired). Open suggestions: euros (27), fonts (26, judged test: still needed), agent UI on Lumble (25, the owner's). Test runs: 1–2 failed (rule 1; its request now resolves to the real 8.8k-character foundation brief — **do not press Try again**, it would rebuild the whole foundation), 3–6 judged, 7 waiting for a verdict with the "with the rule" copy `ee2b99af-a5c1-45ba-9eb0-4239f8f88ce0` kept in the workspace. Workspace Knowledge is empty; no workspace Skills (a test skill was created, changed and deleted).

## 6. What is NOT built (and the owner knows)

- Behavioural checks against test copies (see 4b). Reviewer model; Scoreboard (needs weeks of data).
- Hosted autonomy: Lovable rejected the hosted OAuth client ("Client Not Found"); the hosted Supabase half is a schema of the vision with no working pipeline; leave it.
- Not exercised live: Connect/Disconnect (needs the owner's browser consent — see §9), the Instructions pending-write "Cancel" (needs a failed write), "hurt"/"unused" retirement reasons (need weeks of builds).

## 7. How the owner wants work done (binding)

- Sonnet 5 is the ceiling for subagents; Haiku for mechanical work; never Opus (a rate limit cut a review short). Use subagents freely; implementers never spawn their own reviewers.
- Question the setup instead of silently working around it; say plainly what does not make sense.
- Fewer things on screen. Do not hide capability behind stubs; show it with honest copy. Don't over-promise: say what is automatic and what needs a human.
- Anything that spends **Lovable credits** (send_message, create/remix project, skill writes) needs the owner's explicit go-ahead, every time. Reading via Lovable MCP is free and fine. LLM calls only on "Analyse now" / `--analyse`.
- Keys never in SQLite, responses or logs; tokens only in `harness/data/lovable-auth.json`; never read `~/.claude`.
- Every `<details>` collapsed; `lovableStatusLine` is the only source of "added to Lovable" claims; the structural tests in `harness/test/ux*.test.ts` read source files as text and pin copy — update them with intent, don't weaken them.
- **Update this handoff at the end of every round, unasked.** Commit only when the owner says so.
- For live tests the owner sets a hard credit cap per session (Round 7: 15). Track every `cost_credits`.
- Commits end with the `Co-Authored-By` / `Claude-Session` trailer used throughout the branch. Work in place on `local-harness-dev`; the owner runs the dev server from this checkout.
- Process that worked: spec → plan → subagent per task with a brief → per-task review → scoped re-review of fixes → whole-branch review → one fix wave. Keep a ledger under `.superpowers/sdd/<plan>/progress.md` while a plan runs and delete it when done. Parallel implementers are fine on disjoint files, but make **one task own each migration** and tell agents to append store code in delimited blocks and re-read before editing; the shared git index caused several near-misses (agents used hunk-level staging to recover).

## 8. Lessons from this session (so you don't repeat them)

- **Never run `git stash`, `git reset` or `git checkout -- <file>` in this checkout while other agents work in it**: one agent's `git stash` reset the shared tree three times in Round 6 and path-limited commits repeatedly swept another agent's uncommitted hunks into the wrong commit (content was always recovered; attribution is off in several commits). Give parallel agents disjoint files or run them sequentially.
- Demo data recorded under the real project id poisoned real writes (see §5); Round 6 Task 5 closed that path.
- Subagents on Sonnet hit the session rate limit at ~23:30 UTC on 2026-09-12; Task 6b died before writing anything.

- The single most valuable review was the whole-branch one that found the strict-schema bug: mocked `fetch` tests cannot catch provider-contract violations. `npm run llm:smoke` (Claude Code, temp DB, one tiny call) is the live check — run it after touching `harness/src/llm/`.
- "Simplifying" by hiding empty pages read to the owner as removing features. Show state honestly instead.
- Reviewers will flag things the plan itself mandated (a wrong derivation of `test_first`, a copy line promising a feature). Rule on them; don't dismiss them because the plan said so.
- Two data models (hosted Supabase vs local SQLite) still exist; every new feature went local-only on purpose.
- Mocked tests passed while the real app failed in eight places: contract mismatches (route vs page field names), the MCP `(empty)` placeholder, the REST `limit<=50`, Lovable's changed reply format, and SQLite-vs-ISO timestamps. Drive the real UI against real Lovable before calling a flow done.
- Don't delete sonner toast DOM nodes from a Playwright script (React crashes); run `npm --prefix harness run executor -- --analyse` with an **absolute** `HARNESS_DB_PATH` (a relative one silently creates a fresh DB under `harness/harness/`).
- Lovable's `list_messages` returns the builder's whole activity log inside assistant `content` (tens of KB); always reduce to the human-visible part (`humanVisibleText`) before sending to any model.

## 9. Immediate next steps, in order

1. Restart the owner's dev server (port 8081) so the app runs Round 7 code; ask whether to commit Round 7 (and merge/PR the branch).
2. With the owner: judge test 7 (a kept copy to open and continue from), and decide on the memory confounder (4b).
3. Design behavioural checks for non-visual rules (the owner's login example) — spec first.
4. Analyse-now progress feedback; a "Removed from Knowledge" History node for retirements.
5. Connect/Disconnect by the owner: back up `harness/data/lovable-auth.json` first (`cp harness/data/lovable-auth.json /tmp/lovable-auth.backup.json`), forward port **8765** to the laptop (VS Code "Ports" → Forward a Port → 8765) because the OAuth callback listens on `127.0.0.1:8765` on the server, then Projects › Disconnect → Connect Lovable → approve in the browser tab → the page flips to "Connected". If it fails, restore the backup file and restart the app. CLI alternative on the server: `npm run harness:executor -- --connect`.
6. Parked from Round 6 reviews (still deferrable): timeout branch closing the MCP client mid-call; `activeExperimentRun` second-queued edge case; duplicated card header markup; `retireRule` inline template; `rule_adherence.llm_call_id` always null.
